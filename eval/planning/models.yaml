# QFinZero Multi-Step Planning Eval — Model Endpoints
# =====================================================
# Add model endpoints to evaluate here.
# Same format as eval/calling/models.yaml.

models:
  # --- Remote hosted models ---
  # - model_name: gpt-4.1
  #   base_url: https://api.openai-proxy.com/v1
  #   api_key: "${OPENAI_PROXY_API_KEY}"
  #   provider_type: openai_compatible

  # - model_name: gpt-4.1-mini
  #   base_url: https://api.openai-proxy.com/v1
  #   api_key: "${OPENAI_PROXY_API_KEY}"
  #   provider_type: openai_compatible

  # - model_name: deepseek-chat
  #   base_url: https://api.deepseek.com/v1
  #   api_key: "${DEEPSEEK_API_KEY}"
  #   provider_type: openai_compatible

  # --- Local vLLM models ---
  - model_name: Qwen/Qwen2.5-7B-Instruct
    base_url: http://localhost:8000/v1
    api_key: null
    provider_type: vllm_openai_compatible
